import Logo from '../../../public/logo.svg';

const Loading = () => {
  return (
    <>
      <div className="w-full h-screen">
        <img src={Logo} />
      </div>
    </>
  );
};

export default Loading;
